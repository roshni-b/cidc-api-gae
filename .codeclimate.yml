version: "2"
exclude_patterns:
  - "migrations/"
  - "tests/"
checks:
  method-complexity:
    config:
      threshold: 10
  # Don't complain if files are too long
  file-lines:
    enabled: false
  # This check doesn't work well with python3
  duplication:
    enabled: false
